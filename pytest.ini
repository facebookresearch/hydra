[pytest]
# reasons for excludes:
# .nox: large and irrelevant
# plugins: tested separately
# tutorial: contains the test for the example app, tested separately (app needs to be installed first)
norecursedirs = .nox plugins tutorial