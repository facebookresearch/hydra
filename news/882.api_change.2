Instantiated objects parameters no longer have params in the structure. keys like hydra.launcher.params.foo are changed to hydra.launcher.foo. Update your configs and overrides if you were overriding such parameters.
