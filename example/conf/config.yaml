---
defaults:
    - override hydra/sweeper: wandb
hydra:
    sweeper:
        wandb_sweep_config:
            name: test
            metric:
                name: val_loss
                goal: minimize
            method: bayes
            num_agents: 2
            preemptible: true

x: 1
y: 1
