database: support
tables:
  - name: users
    fields:
      - name: string
      - password: string
  - name: issues
    fields:
      - title: string
      - description: string
      - reported_by: string
      - priority: int
