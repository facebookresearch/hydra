defaults:
  - override hydra/sweeper: ax

polynomial:
  coefficients: ???

hydra:
  sweeper:
    ax_config:
      max_trials: 10

      experiment:
        minimize: true

      early_stop:
        max_epochs_without_improvement: 2
