hydra:
  sweeper:
    params:
      ax_config:
        params:
          quadratic_args_x:
            type: range
            bounds: [-1, 1]
          quadratic_args_y:
            type: range
            bounds: [-1, 1]